{# 4 of the row's 12 columns. The .sparkline class is what the stylesheet hooks onto to fix
   the library's mask stroke and to size the SVG - see app.css. #}
<div{{ attributes.defaults({
	class:'sparkline col-span-4'
}) }}>
	{{ this.sparkLine|raw }}
</div>
