<div{{ attributes.defaults({
    class:'container-fluid d-flex align-items-start gap-2'
}) }}>
    <h4 class="fw-bold mb-0 text-break">{{ record.message }}</h4>
    {% include '@BugCatcher/components/_copy_button.html.twig' with {
        text: record.message,
        label: 'Copy error message'|trans({},"BugCatcher"),
    } only %}
</div>
